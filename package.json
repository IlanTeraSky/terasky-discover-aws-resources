{
  "name": "terasky-discover-aws-resources",
  "version": "1.0.0",
  "description": "A command-line tool to discover your aws services and resources",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "pkg-binaries": "npx pkg . -t node14-linux-x64,node14-macos-x64,node14-win-x64 --out-path ./dist/binaries"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.1450.0"
  },
  "devDependencies": {
    "@types/node": "^20.5.9"
  },
  "pkg": {
    "scripts": [
      "dist/**/*.js"
    ]
  }
}
